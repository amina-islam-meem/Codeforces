#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n ; cin >> n;
    if(n == 15 || n==20 || n == 21)
        cout << "No" << endl;
    else
        cout << "YES" << endl;
    return 0;
}
