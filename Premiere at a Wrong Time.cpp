#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

        string s; cin >> s;
        if(s=="first")
            cout << "Nauuo and Votes" <<endl;
        else
            cout << "Nauuo and Chess" << endl;

    return 0;
}
